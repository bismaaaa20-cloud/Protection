-- GLOBAL SETTINGS (Format murni Lua - VERSI PENYEMPURNAAN HITBOX & DODGE)
getgenv().FastEnergyRegen = true       -- Pengisian energi kilat (Bisa Dash & Melompat Lancar)
getgenv().AntiPlayerComboShield = true -- Desync anti-combo PvP (Hitbox pindah ke bawah tanah)
getgenv().ComboBreaker = true          -- Lepas dari status kaku / stun skill lawan
getgenv().SuperSmallHitbox = true      -- Hitbox super kecil (0.1) agar serangan musuh meleset total
getgenv().LowHealthSkyDodge = true     -- Menghindar otomatis universal saat darah kritis

local Player = game.Players.LocalPlayer
local LastDamageTime = 0
local ShieldActive = false
local EscapeActive = false

-- =========================================================
-- 1. LOOP: FAST REGEN ENERGY (Bisa Dash & Melompat Lancar)
-- =========================================================
task.spawn(function()
    while task.wait(0.01) do
        local Character = Player.Character
        if Character then
            local EnergyObj = Character:FindFirstChild("Energy") or Player:FindFirstChild("Data") and Player.Data:FindFirstChild("Energy")
            if getgenv().FastEnergyRegen and EnergyObj and EnergyObj:IsA("IntValue") then
                if EnergyObj.Value < EnergyObj.MaxValue then
                    EnergyObj.Value = math.min(EnergyObj.MaxValue, EnergyObj.Value + 500)
                end
            end
        end
    end
end)

-- =========================================================
-- 2. LOGIKA UTAMA PROTEKSI UNIVERSAL & NEW DODGE PATTERN
-- =========================================================
local function AturSistemPVP(Character)
    local Humanoid = Character:WaitForChild("Humanoid")
    local RootPart = Character:WaitForChild("HumanoidRootPart")
    local LastHealth = Humanoid.Health

    -- PERBAIKAN STABILIZER LANTAI AGAR KAKI TIDAK NEMBUS
    if getgenv().SuperSmallHitbox and RootPart and Humanoid then
        RootPart.Size = Vector3.new(0.1, 0.1, 0.1)
        
        -- Mengunci HipHeight secara berulang melalui task.spawn agar tidak ditimpa oleh animasi gaya bertarung
        task.spawn(function()
            while Character and Character.Parent and getgenv().SuperSmallHitbox do
                if Humanoid.HipHeight ~= 3.5 then
                    Humanoid.HipHeight = 3.5 -- Nilai dinaikkan ke 3.5 agar kaki dijamin mengambang di atas lantai
                end
                task.wait(0.5)
            end
        end)
    end

    Humanoid.HealthChanged:Connect(function(CurrentHealth)
        local CurrentTime = tick()
        
        -- REKAYASA BARU: SIDE-TO-SKY DODGE (ANTI-CHEAT BYPASS & REAL HITBOX ESCAPE)
        if getgenv().LowHealthSkyDodge and CurrentHealth < (Humanoid.MaxHealth * 0.3) and not EscapeActive and CurrentHealth > 0 then
            EscapeActive = true
            task.spawn(function()
                print("[Dodge] Darah kritis! Menghindar ke samping lalu meluncur ke langit...")
                
                -- Memasang penahan gravitasi pasif
                local BodyVelocity = Instance.new("BodyVelocity")
                BodyVelocity.Velocity = Vector3.new(0, 0, 0)
                BodyVelocity.MaxForce = Vector3.new(0, math.huge, 0)
                BodyVelocity.Parent = RootPart
                
                -- TAHAP 1: Menghindar ke Kanan secara horizontal sejauh 25 stud untuk keluar dari target area musuh
                RootPart.CFrame = RootPart.CFrame * CFrame.new(25, 0, 0)
                task.wait(0.02)
                
                -- TAHAP 2: Meluncur naik vertikal di tempat yang baru secara halus agar lolos anti-cheat
                for i = 1, 18 do
                    RootPart.CFrame = RootPart.CFrame * CFrame.new(0, 8, 0)
                    task.wait(0.01)
                end
                
                -- TAHAP 3: MENAHAN POSISI LEBIH LAMA (Dinaikkan dari 4 detik menjadi 7 detik sesuai permintaan)
                task.wait(7.0)
                
                -- TAHAP 4: TURUN DENGAN MEMBACA TINGGI LANTAI BARU (Anti-Amblas & Anti-Jatuh Bebas)
                -- Menggunakan Raycasting lokal untuk mendeteksi koordinat permukaan lantai di bawah karakter
                local RaycastParam = RaycastParams.new()
                RaycastParam.FilterDescendantsInstances = {Character}
                RaycastParam.FilterType = Enum.RaycastFilterType.Exclude
                
                local HasilRay = game.Workspace:Raycast(RootPart.Position, Vector3.new(0, -500, 0), RaycastParam)
                if HasilRay then
                    -- Menurunkan posisi tepat di atas permukaan lantai baru yang terdeteksi oleh sistem
                    RootPart.CFrame = CFrame.new(HasilRay.Position + Vector3.new(0, 4, 0))
                end
                
                BodyVelocity:Destroy()
                EscapeActive = false
                print("[Dodge] Kembali ke lantai baru dengan aman.")
            end)
        end

        -- DETEKSI HIT MASUK
        if CurrentHealth < LastHealth then
            local rawDamage = LastHealth - CurrentHealth
            
            -- ANTI ONE-SHOT / COMBO SHIELD (Desync Hitbox)
            if getgenv().AntiPlayerComboShield and (CurrentTime - LastDamageTime) < 1.0 then
                if not ShieldActive then
                    ShieldActive = true
                    task.spawn(function()
                        local SimpanCFrame = RootPart.CFrame
                        while ShieldActive do
                            RootPart.CFrame = SimpanCFrame * CFrame.new(0, -20, 0) 
                            task.wait()
                        end
                        RootPart.CFrame = SimpanCFrame 
                    end)
                    task.delay(1.5, function() ShieldActive = false end)
                end
            end
            
            -- ANTI-STUN UNIVERSAL (Combo Breaker)
            if getgenv().ComboBreaker and (CurrentTime - LastDamageTime) < 1.2 then
                Humanoid.PlatformStand = false
                Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                if RootPart.Anchored then RootPart.Anchored = false end
            end
            
            LastDamageTime = CurrentTime
        end
        LastHealth = Humanoid.Health
    end)
end

if Player.Character then task.spawn(AturSistemPVP, Player.Character) end
Player.CharacterAdded:Connect(function(NewCharacter)
    string.clear = nil
    task.spawn(AturSistemPVP, NewCharacter)
end)
